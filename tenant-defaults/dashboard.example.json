{
  "version": 1,
  "pages": [
    {
      "slug": "family",
      "label": "Family",
      "description": "Open action items with domain = family (requires brain.db only)",
      "template": "action_domain",
      "domain": "family"
    },
    {
      "slug": "demo-query",
      "label": "Demo row",
      "description": "Example datatable page — runs one read-only SELECT",
      "template": "datatable",
      "db": "brain",
      "sql": "SELECT 1 AS n, 'ok' AS status"
    },
    {
      "slug": "money",
      "label": "Money overview",
      "description": "Example multi-database `sections` page — brain action items + finance.db table (use any slug; section ids must be unique per page)",
      "template": "sections",
      "sections": [
        {
          "id": "todos",
          "label": "Todos",
          "template": "todos",
          "domain": "finance"
        },
        {
          "id": "recent-tx",
          "label": "Recent transactions",
          "template": "datatable",
          "db": "finance",
          "sql": "SELECT date, category, amount FROM transactions ORDER BY date DESC LIMIT 50"
        }
      ]
    }
  ]
}
